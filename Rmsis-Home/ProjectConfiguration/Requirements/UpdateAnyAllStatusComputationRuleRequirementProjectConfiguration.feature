Feature: Update the Any/All column in Status Computation rules for Requirements in project configuration

Scenario: Update the Any/All column in Status Computation rules for Requirements in project configuration
   Given I Click in the Any/All column in Status computation rules for requirements in the Requirements tab    
   When I Select the value from the drop down
   Then Any/All column should be updated