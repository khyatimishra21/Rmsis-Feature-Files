Feature: Mark the status rule as enable in Status Computation rules for Requirements in project configuration

Scenario: Mark the status rule as enable in Status Computation rules for Requirements in project configuration
   When I Mark the check box in the enable column in Status computation rules for requirements in the Requirements tab  
   Then Status Rule should be marked as enable