Feature: Export the report as a JSON for custom reporting
   
Scenario: Export the report as a JSON for custom reporting              	
   When I Click on Export as JSON option
   Then The report should be exported as JSON