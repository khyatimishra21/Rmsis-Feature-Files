Feature: Mark the status rule as enable in Status Computation rules for Requirements
   Mark the status rule as enable in Status Computation rules for Requirements with valid dataset

Scenario: Mark the status rule as enable in Status Computation rules for Requirements
   When I Mark the check box in the enable column in Status computation rules for requirements
   Then Status Rule should be marked as enable